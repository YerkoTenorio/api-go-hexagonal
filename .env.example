# Entorno de la aplicación
APP_ENV=development        # development | production
APP_NAME=Task Manager API
LOG_LEVEL=info

# Servidor
SERVER_HOST=localhost      # usar 0.0.0.0 para exposición externa
SERVER_PORT=8080           # cambia si 8080 está en uso

# Base de datos local (SQLite)
DB_PATH=./data/tasks.db

# Base de datos remota (Turso / libSQL)
# Deja DB_URL vacío si usas SQLite local
DB_URL=
DB_AUTH_TOKEN=